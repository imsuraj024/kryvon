name: kryvon
description: "Runtime security assessment and policy engine for Flutter applications."
version: 0.3.0
homepage: https://github.com/imsuraj024/kryvon

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
